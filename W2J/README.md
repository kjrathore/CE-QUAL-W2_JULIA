# W2J — CE-QUAL-W2 in Julia

See [`README.md` at the repo root](../README.md) for the Julia package documentation
(package structure, module status, build/test instructions, progress tracker) and
[`CLAUDE.md` at the repo root](../CLAUDE.md) for session-to-session working notes.

This file used to hold a full standalone copy of that documentation (drafted in an
early session before a Julia runtime was available to verify anything). It diverged
from the root `README.md` as work progressed and was replaced with this pointer on
2026-08-15 to avoid maintaining two copies that silently go out of sync again.
